<div class="container">
  <h1>Welcome to Opinionated Guerilla!</h1>

  <p>Number of surveys taken today:</p>
    <%= CompletedSurvey.where("created_at > ?", DateTime.now.beginning_of_day).count %>

  <h3>Take a current survey:</h3>
  <ul>
    <% Survey.all.each do |survey| %>
    <a href="/survey/<%= survey.id %>/take"><li><%= survey.title %></li></a>
    <% end %>
  </ul>
</div>